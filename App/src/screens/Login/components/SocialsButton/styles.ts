import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    width: '80%',
    flexDirection: 'row',
    borderWidth: 1,
    alignItems: 'center',
    justifyContent: 'space-around',
    paddingVertical: 8,
    borderRadius: 50,
    marginVertical: 15,
  },

  textButton: {
    fontSize: 20,
  },
});
